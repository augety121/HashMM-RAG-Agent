# HashMM App V247 —— 接力页按钮改安静款（配套桌面端完整源码 V249）

## 改动
- **接力页「转到桌面端」**：每行一个墨黑实心大胶囊太重（满屏黑块）——改为
  浅灰容器 + 墨黑字的安静胶囊（radius 50、图标 15dp）；无在线主机时整体降灰，
  可用性一眼可判。语义色只留给状态（在线绿点 / 失败红）。

## 说明
- 桌面端本轮交付的是**完整源码包 V249**（你的 V248 + 全部修改与新增合并），
  不再是覆盖包——解压后直接按 installer-native 原流程打包。
- V246 的记忆搜索 / ＋多智能体等入口在打完 V249 后端后全部可用。
