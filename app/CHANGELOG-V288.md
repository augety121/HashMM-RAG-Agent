# HashMM App 1.10.64（105）

日期：2026-07-19

- App Chat 与桌面端使用同一会话富流式主链，任务契约、工具轨迹、活动轮次和最终消息不再被旧移动端接口割裂。
- 长任务运行中输入框保持可用：发送键追加真实用户指令，停止键协作式中断精确轮次；旧轮次不能停止或修改新任务。
- 不确定的网络失败先回读服务端历史，发现服务端已受理时不再自动重发，避免重复消息、重复计费和重复工具副作用。
- 活动轮次状态通过 SSE 语义事件恢复；重新进入 Chat 后仍以服务端状态为准，不使用本地演示状态。
- 联动 Backend V360、Desktop/MCP/Native 1.17.16。

## 验证

- `testDebugUnitTest`：133 个测试通过，Kotlin 编译通过。
- APK：`HashMM-App-1.10.64-debug.apk`，70,317,643 bytes。
- SHA-256：`0447546cefa332ea508073460c14cdf347ec709e0a58d92bb6f45fe818729812`。
- Debug APK 未使用正式发布签名，仅用于本轮安装验证。

---

# HashMM App V288

日期：2026-07-19

- 工作台“上下文透视”新增真实的长对话手动压缩操作，调用服务端属主校验接口并绑定当前会话，不再是本地演示状态。
- 压缩完成后刷新同一会话的上下文统计，显示新的检查点状态；完整历史消息保留，不删除 Room、SQLite 或 Supabase 原文。
- 服务端拒绝、网络错误和无需新建检查点分别显示真实结果，不使用虚构成功数据。
- 版本升级为 versionCode 104、versionName 1.10.63；联动 backend V359。

## 验证

- `testDebugUnitTest`、Kotlin 编译、`assembleDebug` 通过。
- APK：`HashMM-App-1.10.63-debug.apk`，70,301,259 bytes。
- SHA-256：`2784101ff6981699124c11747e0a7a02a051a59dead610797f78725db4d3a52f`。
- Debug APK 未使用正式发布签名，仅用于本轮安装验证。
