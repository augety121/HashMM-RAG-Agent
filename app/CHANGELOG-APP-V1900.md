# HashMM App V1900

- 版本：7.0.0（versionCode 190）。
- 设备页不再把 `hashmm.remote.v2` 协议协商成功显示成“已有电脑可连接”。
- 只有设备列表中存在 `remote_ready=true` 的同账号主机时才显示“已发现可远程电脑/可连接”。
- 通道已连接但主机未注册时显示“账号通道已连接，未发现可远程电脑/等待电脑”。
- 保留既有 App 导航和整体界面结构；更新了远程状态语义与回归契约。

验证：Kotlin 编译、184 项 Debug 单元测试与 Debug APK 构建通过。
