# HashMM App 5.0.0 / Backend V1700

- 连接状态从“后端能访问”升级为“传输可达 + Supabase 项目一致 + 用户 UUID 指纹一致”。
- 远程配置失败不再抑制六小时重试；提供从手动覆盖恢复自动配置的入口。
- 历史对话失败保留账号隔离缓存，并区分真实空列表、离线、登录过期、项目不一致和协议不兼容。
- 历史抽屉加入搜索与时间分组，去掉厚重图标底色，增加同步状态和稳定选中态。
- 执行方式统一为智能选择、服务器工作区、手机快速回答；手机直连只有收到后端写入回执才显示已进入工作区。
- 版本：5.0.0（versionCode 170）。QQ 登录不在本版本范围内。
