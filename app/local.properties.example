# 复制为 local.properties（不要提交）。Android SDK 路径 + Supabase 配置。
sdk.dir=/path/to/Android/sdk

# 与后端同一个 Supabase 项目（publishable key 可公开，编进 APK）
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx
# 默认后端地址（留空则首次进 App 在「设置」里填）
DEFAULT_CLIENT_URL=
