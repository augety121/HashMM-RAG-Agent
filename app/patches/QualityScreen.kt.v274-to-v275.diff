--- app_pr/a/app/src/main/java/com/hashmm/app/ui/workbench/QualityScreen.kt	2026-07-11 02:37:37.000000000 +0000
+++ app/HashMM-App-V268/app/src/main/java/com/hashmm/app/ui/workbench/QualityScreen.kt	2026-07-11 03:49:47.254780741 +0000
@@ -33,10 +33,10 @@
 @Composable
 fun QualityScreen(onBack: () -> Unit, viewModel: AdminToolsViewModel = hiltViewModel()) {
     val scope = rememberCoroutineScope()
-    var data by remember { mutableStateOf<QualityData?>(null) }
+    var data by remember { mutableStateOf(WorkbenchCache.quality) }   // V275 跨导航持久化：先显上次
     var refreshing by remember { mutableStateOf(false) }
-    fun reload() { scope.launch { refreshing = true; data = viewModel.quality(); refreshing = false } }
-    LaunchedEffect(Unit) { reload() }
+    fun reload() { scope.launch { refreshing = true; data = viewModel.quality().also { WorkbenchCache.quality = it }; refreshing = false } }
+    LaunchedEffect(Unit) { if (WorkbenchCache.quality == null) reload() }   // V275 有缓存不空转重拉
     val d = data
     Column(Modifier.fillMaxSize().background(MaterialTheme.colorScheme.background)) {
         ModuleHeader(Icons.Outlined.BarChart, "质量看板", "近 7 天关键指标（与电脑端同源）", onBack)
