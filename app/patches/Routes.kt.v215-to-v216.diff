--- /tmp/app-v215-base/src/main/java/com/hashmm/app/ui/Routes.kt	2026-07-03 10:17:15.421076951 +0000
+++ app/src/main/java/com/hashmm/app/ui/Routes.kt	2026-07-03 10:17:15.474658273 +0000
@@ -9,6 +9,8 @@
     const val MEMORY = "memory"                   // 记忆中心（云端同步）
     const val KNOWLEDGE = "knowledge"             // 知识库（直连客户端后端）
     const val WEB_WORKBENCH = "web_workbench"     // 网页版完整客户端（WebView 兜底）
+    const val WEB_WORKBENCH_ROUTE = "web_workbench?view={view}"   // V216: 可带 view 直达某桌面面板
+    fun webWorkbench(view: String = "") = "web_workbench" + (if (view.isNotBlank()) "?view=$view" else "?view=")
     const val MODELS = "models"                   // 模型/后端配置
     const val KG = "kg"                           // 知识图谱
     const val ADMIN = "admin"                     // 管理后台
