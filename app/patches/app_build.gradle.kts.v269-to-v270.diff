--- app_pr/HashMM-App-V269/app/build.gradle.kts	2026-07-10 09:15:58.000000000 +0000
+++ app/HashMM-App-V268/app/build.gradle.kts	2026-07-10 13:18:58.236028454 +0000
@@ -26,8 +26,8 @@
         applicationId = "com.hashmm.app"   // ← 独立包名，和 HashLens(com.hashlens.app) 并存，不覆盖
         minSdk = 26
         targetSdk = 36
-        versionCode = 84
-        versionName = "1.10.43"
+        versionCode = 85
+        versionName = "1.10.44"
 
         // Supabase（仅用于登录鉴权；publishable key 可公开，配合后端校验）
         buildConfigField("String", "SUPABASE_URL", "\"${localProp("SUPABASE_URL")}\"")
@@ -101,6 +101,10 @@
     implementation(libs.coroutines.android)
     implementation(libs.serialization.json)
 
+    // V270 单元测试（app/src/test，纯 JVM）：JUnit4。coroutines-core 经 implementation 已对
+    // 单测编译类路径可见（runBlocking/launch/CompletableDeferred 直接可用），无需 coroutines-test。
+    testImplementation(libs.junit)
+
     // OkHttp（WebSocket 信令）
     implementation(libs.okhttp.core)
     implementation(libs.okhttp.logging)
