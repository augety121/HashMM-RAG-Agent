--- /tmp/b-Color.kt	2026-07-04 13:20:29.850620727 +0000
+++ app/src/main/java/com/hashmm/app/ui/theme/Color.kt	2026-07-04 13:16:35.824432068 +0000
@@ -10,18 +10,18 @@
 //  说明：本文件只换"值"不换"名"——其它文件 import 的 Brand/Accent/AvatarGradient… 都不变，全局自动生效。
 // ════════════════════════════════════════════════════════════════
 
-// 主色：品牌红（V242 大厂化微调——略降饱和更沉稳，减少满屏刺眼感，仍贴合红围巾品牌）
-val Brand = Color(0xFFE23D35)            // 围巾红 —— 全 App 强调色（沉一档）
-val BrandDark = Color(0xFFC7332C)        // 深一档（按下 / 深容器 / 强调文字）
-val BrandLight = Color(0xFFF15A50)       // 深色主题主色（亮一档，近黑底上更醒目）
-val BrandContainer = Color(0xFFFCE4E2)   // 浅红容器（更淡更柔）
-val Violet = Color(0xFF7A1A16)           // 深红（渐变深端 / 兼容旧名保留）
-val Accent = Color(0xFFE23D35)           // 强调色＝品牌红
-val AccentSoft = Color(0xFFFCE4E2)       // 浅红容器
+// 主色：靛蓝 indigo（V246 换色——大厂科技感、沉稳耐看，替原满屏红；改值不改名全局生效）
+val Brand = Color(0xFF4F46E5)            // indigo-600 —— 全 App 强调色
+val BrandDark = Color(0xFF4338CA)        // indigo-700（按下 / 深容器 / 强调文字）
+val BrandLight = Color(0xFF6366F1)       // indigo-500（深色主题主色，近黑底上更醒目）
+val BrandContainer = Color(0xFFE7E7FB)   // 浅靛容器（浅色主题 primaryContainer）
+val Violet = Color(0xFF3730A3)           // indigo-800（渐变深端 / 兼容旧名保留）
+val Accent = Color(0xFF4F46E5)           // 强调色＝靛蓝
+val AccentSoft = Color(0xFFE7E7FB)       // 浅靛容器
 
-// 渐变：品牌红（logo 块、头像）——收敛一点，端点不那么亮
-val BrandGradient = Brush.linearGradient(listOf(Color(0xFFF15A50), Color(0xFFE23D35)))
-val AvatarGradient = Brush.linearGradient(listOf(Color(0xFFF15A50), Color(0xFFC7332C)))
+// 渐变：靛蓝（logo 块、头像、主 CTA）——柔和科技感
+val BrandGradient = Brush.linearGradient(listOf(Color(0xFF6366F1), Color(0xFF4F46E5)))
+val AvatarGradient = Brush.linearGradient(listOf(Color(0xFF6366F1), Color(0xFF4338CA)))
 
 // 浅色系：白底 + 极浅中性灰；深色文字用近黑（=吉祥物机身 #16161A），不用纯黑
 val LightBg = Color(0xFFF6F6F7)          // 页面底：极浅中性灰（大厂常用的柔和底）
