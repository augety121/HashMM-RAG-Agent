--- /tmp/b-AdvancedScreen.kt	2026-07-04 00:07:44.218230906 +0000
+++ app/src/main/java/com/hashmm/app/ui/workbench/AdvancedScreen.kt	2026-07-04 00:07:05.930366910 +0000
@@ -162,6 +162,10 @@
                                 val (okd, msg) = viewModel.dispatch("watch_stop", org.json.JSONObject().put("all", true))
                                 lastMsg = if (okd) "✓ $msg" else msg
                             } }) { Text("全部撤岗", fontSize = 12.5.sp) }
+                            TextButton(onClick = { scope.launch {
+                                val (okd, msg) = viewModel.dispatch("watch_list", org.json.JSONObject())
+                                lastMsg = if (okd) msg else msg
+                            } }) { Text("查看哨兵", fontSize = 12.5.sp) }
                         }
                     } }
                     item { SectionCard(Icons.Outlined.Image, "会话图库",
