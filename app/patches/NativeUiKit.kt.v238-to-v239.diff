--- /tmp/b-NativeUiKit.kt	2026-07-04 16:56:02.345366355 +0000
+++ app/src/main/java/com/hashmm/app/ui/workbench/NativeUiKit.kt	2026-07-04 16:55:29.503042184 +0000
@@ -133,18 +133,19 @@
     Surface(
         color = MaterialTheme.colorScheme.surface, shape = RoundedCornerShape(16.dp),
         border = BorderStroke(1.dp, MaterialTheme.colorScheme.outlineVariant.copy(alpha = 0.55f)),
+        shadowElevation = 1.dp,
         modifier = Modifier.fillMaxWidth(),
     ) {
         Row(Modifier.height(IntrinsicSize.Min), verticalAlignment = Alignment.CenterVertically) {
-            // 左侧 3dp 强调色条——大�ilds厂列表项的"分类感"来源
-            Box(Modifier.width(3.dp).fillMaxHeight().padding(vertical = 12.dp)
+            // 左侧 3dp 强调色条——大厂列表项的"分类感"来源
+            Box(Modifier.width(3.dp).fillMaxHeight().padding(vertical = 13.dp)
                 .background(toneColor(tone), RoundedCornerShape(2.dp)))
-            Row(Modifier.padding(start = 12.dp, end = 14.dp, top = 12.dp, bottom = 12.dp),
+            Row(Modifier.padding(start = 13.dp, end = 15.dp, top = 14.dp, bottom = 14.dp),
                 verticalAlignment = Alignment.CenterVertically) {
                 Box(
-                    Modifier.size(38.dp).background(toneColor(tone).copy(alpha = 0.13f), RoundedCornerShape(11.dp)),
+                    Modifier.size(40.dp).background(toneColor(tone).copy(alpha = 0.12f), RoundedCornerShape(12.dp)),
                     contentAlignment = Alignment.Center,
-                ) { Icon(icon, null, tint = toneColor(tone), modifier = Modifier.size(19.dp)) }
+                ) { Icon(icon, null, tint = toneColor(tone), modifier = Modifier.size(20.dp)) }
                 Spacer(Modifier.width(13.dp))
                 Column(Modifier.weight(1f)) {
                     Row(verticalAlignment = Alignment.CenterVertically) {
