--- app_pr/a/app/src/main/java/com/hashmm/app/ui/workbench/ContextScreen.kt	2026-07-11 02:37:37.000000000 +0000
+++ app/HashMM-App-V268/app/src/main/java/com/hashmm/app/ui/workbench/ContextScreen.kt	2026-07-11 03:49:47.281248174 +0000
@@ -39,10 +39,10 @@
 @Composable
 fun ContextScreen(onBack: () -> Unit, viewModel: AdminToolsViewModel = hiltViewModel()) {
     val scope = rememberCoroutineScope()
-    var data by remember { mutableStateOf<AdminToolsRepository.CtxInspect?>(null) }
+    var data by remember { mutableStateOf(WorkbenchCache.context) }   // V275 跨导航持久化
     var refreshing by remember { mutableStateOf(false) }
-    fun reload() { scope.launch { refreshing = true; data = viewModel.contextInspect(); refreshing = false } }
-    LaunchedEffect(Unit) { reload() }
+    fun reload() { scope.launch { refreshing = true; data = viewModel.contextInspect().also { WorkbenchCache.context = it }; refreshing = false } }
+    LaunchedEffect(Unit) { if (WorkbenchCache.context == null) reload() }
     val d = data
     Column(Modifier.fillMaxSize().background(MaterialTheme.colorScheme.background)) {
         ModuleHeader(Icons.Outlined.CenterFocusStrong, "上下文透视", "这轮 system 由哪些块组成（与电脑端同源）", onBack)
