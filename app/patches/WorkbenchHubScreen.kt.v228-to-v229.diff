--- /tmp/b-WorkbenchHubScreen.kt	2026-07-04 03:22:07.687446151 +0000
+++ app/src/main/java/com/hashmm/app/ui/workbench/WorkbenchHubScreen.kt	2026-07-04 03:20:56.880740104 +0000
@@ -31,6 +31,7 @@
 import androidx.compose.ui.Modifier
 import androidx.compose.ui.draw.alpha
 import androidx.compose.ui.draw.clip
+import androidx.compose.ui.graphics.Brush
 import androidx.compose.ui.graphics.Color
 import androidx.compose.ui.graphics.vector.ImageVector
 import androidx.compose.ui.text.font.FontWeight
@@ -100,7 +101,8 @@
         val healthScope = rememberCoroutineScope()
         fun probeHealth() { healthScope.launch { val (okk, rel) = healthVm.health(); backendOk = okk; backendRel = rel } }
         LaunchedEffect(Unit) { probeHealth() }
-        Surface(
+        // V237: 正常态零横幅（健康即无感）——仅 探测中/版本旧/不可达 才显示状态条
+        if (!(backendOk == true && backendRel.isNotBlank())) Surface(
             color = when {
                 backendOk == true && backendRel.isNotBlank() -> cs.primary.copy(alpha = 0.08f)
                 backendOk == true -> cs.tertiaryContainer.copy(alpha = 0.45f)
@@ -112,7 +114,6 @@
         ) {
             Row(Modifier.padding(horizontal = 12.dp, vertical = 9.dp), verticalAlignment = Alignment.CenterVertically) {
                 Box(Modifier.size(7.dp).clip(CircleShape).background(when {
-                    backendOk == true && backendRel.isNotBlank() -> cs.primary
                     backendOk == true -> cs.tertiary
                     backendOk == false -> cs.error
                     else -> cs.onSurfaceVariant
@@ -121,7 +122,6 @@
                 Text(
                     when {
                         backendOk == null -> "正在探测后端…"
-                        backendOk == true && backendRel.isNotBlank() -> "后端 $backendRel · 已连接（全部模块可用）"
                         backendOk == true -> "后端已连接 · 版本较旧（≤V217，部分模块降级）——unzip -o 新包升级"
                         else -> "后端不可达 · 点此重试（检查地址/是否启动）"
                     },
@@ -262,9 +262,10 @@
     ) {
         Row(Modifier.fillMaxWidth().padding(13.dp).alpha(alpha), verticalAlignment = Alignment.CenterVertically) {
             Box(
-                Modifier.size(38.dp).clip(RoundedCornerShape(11.dp)).background(cs.primary.copy(alpha = 0.10f)),
+                Modifier.size(40.dp).clip(RoundedCornerShape(12.dp)).background(
+                    Brush.linearGradient(listOf(cs.primary, cs.primary.copy(alpha = 0.78f)))),
                 contentAlignment = Alignment.Center,
-            ) { Icon(icon, title, tint = cs.primary, modifier = Modifier.size(21.dp)) }
+            ) { Icon(icon, title, tint = Color.White, modifier = Modifier.size(21.dp)) }
             Spacer(Modifier.width(13.dp))
             Column(Modifier.weight(1f)) {
                 Text(title, fontSize = 15.sp, fontWeight = FontWeight.SemiBold, color = cs.onSurface)
