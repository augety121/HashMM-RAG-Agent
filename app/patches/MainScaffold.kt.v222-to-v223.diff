--- /tmp/b-MainScaffold.kt	2026-07-03 23:31:37.035156127 +0000
+++ app/src/main/java/com/hashmm/app/ui/MainScaffold.kt	2026-07-03 23:29:53.452945906 +0000
@@ -117,7 +117,7 @@
                     onSendTask = { text -> if (loggedIn) onNewChat(text) else onLogin() }, // 发送 → 新建原生会话并自动发送
                     onComputerTask = { task, kind -> if (loggedIn) onNewChatDispatch(task, kind) else onLogin() }, // 电脑任务 → 下发到电脑客户端
                 )
-                1 -> if (loggedIn) ActivityScreen(onBack = {}, onOpenConversation = onOpenConversation, showBack = false)
+                1 -> if (loggedIn) ActivityScreen(onBack = {}, onOpenConversation = onOpenConversation, showBack = false, onOpenRuns = onOpenRuns, onOpenScheduled = onOpenScheduled)
                      else LoginGate("登录后查看动态与历史对话", "未登录时不展示任何账号数据", onLogin)
                 2 -> if (loggedIn) WorkbenchHubScreen(
                     onOpenKnowledge = onOpenKnowledge,
