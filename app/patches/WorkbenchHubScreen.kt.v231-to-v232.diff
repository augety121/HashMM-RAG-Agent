--- /tmp/b-WorkbenchHubScreen.kt	2026-07-04 11:11:35.771017422 +0000
+++ app/src/main/java/com/hashmm/app/ui/workbench/WorkbenchHubScreen.kt	2026-07-04 11:10:39.373827627 +0000
@@ -262,16 +262,17 @@
     ) {
         Row(Modifier.fillMaxWidth().padding(13.dp).alpha(alpha), verticalAlignment = Alignment.CenterVertically) {
             Box(
-                Modifier.size(40.dp).clip(RoundedCornerShape(12.dp)).background(
-                    Brush.linearGradient(listOf(cs.primary, cs.primary.copy(alpha = 0.78f)))),
+                Modifier.size(40.dp).clip(RoundedCornerShape(12.dp))
+                    .background(cs.primary.copy(alpha = 0.10f)),   // V242 大厂克制：淡色底 + 品牌色图标（替满屏红实心）
                 contentAlignment = Alignment.Center,
-            ) { Icon(icon, title, tint = Color.White, modifier = Modifier.size(21.dp)) }
-            Spacer(Modifier.width(13.dp))
+            ) { Icon(icon, title, tint = cs.primary, modifier = Modifier.size(21.dp)) }
+            Spacer(Modifier.width(14.dp))
             Column(Modifier.weight(1f)) {
-                Text(title, fontSize = 15.sp, fontWeight = FontWeight.SemiBold, color = cs.onSurface)
+                Text(title, fontSize = 15.sp, fontWeight = FontWeight.SemiBold, color = cs.onSurface, letterSpacing = (-0.2).sp)
+                Spacer(Modifier.height(1.dp))
                 Text(subtitle, fontSize = 12.5.sp, color = cs.onSurfaceVariant)
             }
-            Icon(Icons.Outlined.ChevronRight, null, tint = cs.onSurfaceVariant.copy(alpha = 0.7f))
+            Icon(Icons.Outlined.ChevronRight, null, tint = cs.onSurfaceVariant.copy(alpha = 0.5f), modifier = Modifier.size(18.dp))
         }
     }
 }
