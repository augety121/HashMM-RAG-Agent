--- /tmp/b-ActivityScreen.kt	2026-07-03 14:27:48.590000396 +0000
+++ app/src/main/java/com/hashmm/app/ui/activity/ActivityScreen.kt	2026-07-03 14:25:38.812488903 +0000
@@ -15,6 +15,7 @@
 import androidx.compose.foundation.layout.width
 import androidx.compose.foundation.lazy.LazyColumn
 import androidx.compose.foundation.lazy.items
+import androidx.compose.foundation.lazy.itemsIndexed
 import androidx.compose.foundation.shape.CircleShape
 import androidx.compose.foundation.shape.RoundedCornerShape
 import androidx.compose.material.icons.Icons
@@ -158,18 +159,18 @@
                     if (f != null && (f.runners.isNotEmpty() || f.scheduled.isNotEmpty() || f.scheduledCount >= 0)) {
                         item { SectionLabel("电脑端与定时任务") }
                         if (f.runners.isNotEmpty()) item { RunnersCard(f) }
-                        items(f.scheduled, key = { "s" + it.name + it.lastRunSec }) { s -> ScheduledCard(s) }
+                        itemsIndexed(f.scheduled, key = { i, _ -> "s$i" }) { _, s -> ScheduledCard(s) }
                         if (f.scheduled.isEmpty() && f.scheduledCount >= 0) item {
                             TinyNote("定时任务 ${f.scheduledCount} 个（明细需管理员）")
                         }
                     }
                     if (f != null && f.runs.isNotEmpty()) {
                         item { SectionLabel("运行轨迹") }
-                        items(f.runs, key = { "r" + it.ts + it.title.hashCode() }) { r -> RunRow(r) }
+                        itemsIndexed(f.runs, key = { i, _ -> "r$i" }) { _, r -> RunRow(r) }
                     }
                     if (f != null && f.episodes.isNotEmpty()) {
                         item { SectionLabel("系统学到了什么") }
-                        items(f.episodes, key = { "e" + it.createdAtSec + it.summary.hashCode() }) { e -> EpisodeRow(e) }
+                        itemsIndexed(f.episodes, key = { i, _ -> "e$i" }) { _, e -> EpisodeRow(e) }
                     }
                     item { Spacer(Modifier.height(8.dp)) }
                 }
