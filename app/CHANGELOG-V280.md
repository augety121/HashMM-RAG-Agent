# HashMM App V280

日期：2026-07-18

- 工作台“运行轨迹”支持展开单次运行证据，展示任务类型、执行模式、模型、耗时、Token、迭代轮数、停止原因、稳定运行 ID 和真实失败检查。
- 运行状态改为产品化中文语义：检查通过、有限交付、需要处理、已阻塞、等待输入、已中断等；颜色由同一状态映射确定。
- 每条运行记录可回到产生它的原 Chat，运行诊断不再是与对话割裂的只读列表。
- 质量看板增加基于真实抽检数据的判断卡；不可评估继续保持不可评估，不用零分或演示分数替代。
- 质量问题可直接下钻“运行轨迹”；每日趋势使用真实样本量和有据率绘制，不补造缺失数据。
- 上下文透视读取当前账号最近 Chat，可主动切换目标会话、重新请求 `/api/context/inspect?conv_id=...`，并直接打开原对话。
- 新增工作台状态语义、耗时格式和质量判定回归测试。
- 版本：versionCode 96，versionName 1.10.55。

## 验证与交付

- Android 单元测试：121 passed，0 failed，0 skipped。
- `assembleDebug` 通过；APK 元数据核验为 versionCode 96 / versionName 1.10.55。
- APK：`HashMM-App-1.10.55-debug.apk`，SHA-256 `c511e483b2f0a9495ea7f39f2e35ead8c51c8d998799e98166242c791bb047a9`。
- 服务器包沿用 backend V347；独立审计 895 个 ZIP 条目，禁入目录、危险扩展名和路径穿越命中均为 0。
