# App V255 —— 远程按钮清零装饰 + 三工坊直达

## ① 远程「重启/关机」修通（装饰按钮真凶）
根因：App 发 `action:"system"`，被控端协议白名单没有该动作，静默丢弃。
修复：改发协议认可的 `device` 动作（配合客户端 V257 被控端放行），被控端 3 秒
缓冲执行；**危险命令二次确认**——首点按钮变红「确认关机？」，3 秒内再点才执行，
防误触远程关机。其余按钮（显示桌面/展示所有窗口/锁屏/任务管理器等组合键）排查
确认本就连通。

## ② 工作台新增「智能协作」分组
智能体工坊 / 文档工坊 / 总控中枢三个入口，一键直达与桌面端同一套引擎
（webui view 白名单直开对应面板）。

versionCode 69→70，versionName 1.10.28→1.10.29。三文件括号平衡自查通过。
