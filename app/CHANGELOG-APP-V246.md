# HashMM App V246 —— 配套桌面端覆盖包 V249（记忆中枢 · 多智能体 · 容灾链）

> 本版是"桌面端 V249 三大新能力"的手机入口。**先给桌面端打覆盖包**
> （HashMM-桌面端覆盖包-V249.zip，解压覆盖到源码根后按 installer-native 原流程重打包），
> 否则下述新入口会提示"后端过旧（需 V249+）"。

## ① 记忆中心：联邦记忆搜索（cognee / codebase-memory-mcp 借鉴）
- 页顶新增白色搜索条：一条查询同时召回 **长期记忆（MemoryService）+ 经验回放 +
  用户画像 + 图谱实体** 四路（后端 GET /api/memory/recall）。
- 结果一张分组白卡：来源徽章（长期记忆＝米色、其余灰）+ 文本；输入回车或点「召回」触发，
  搜索中转圈、可一键清除。
- 新增 `data/remote/MemoryHubRepository.kt`（与动态页同一套 base()/token 范式，
  旧后端 404 给可行动提示）。

## ② 高级能力：＋多智能体（herdr 借鉴）
- 「派一个任务」新增 **＋多智能体** 胶囊（kind=team）：协调者把目标拆成 2-4 个角色并行执行，
  **画布就是控制室**（⚪等待 🟡执行中 🟢完成 🔴失败 实时着色），汇总自动回帖会话。
- 弹窗给了明确副题与示例占位（"调研三款竞品并给出选型建议"）。

## ③ 顺带
- 定时任务/管理员相关提示沿用 V245 的可行动文案；打完 V249 覆盖包后应能直接看到明细。
- 模型容灾链是纯后端能力（settings『model_fallbacks』+ /api/admin/model-route/*），
  App 零改动即受益：后台聊天/派活的 LLM 调用在主模型故障时自动切换。

## 验证
- 静态自检通过（3 个改动/新增文件括号配平、关键符号核对、SettingsStore 包路径与
  FeedRepository 一致）。容器内无法跑 Gradle，请构建后回归：记忆中心搜索、
  高级能力派 team 后到桌面端画布看四色直播。
