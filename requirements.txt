# Pinned-ish requirements for reproducibility. The pyproject.toml extras
# are the source of truth; this file mirrors them for `pip install -r`.
# For 4090 + CUDA 12.x, install torch first from the official wheel index:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121

# Core
numpy>=1.24,<3
pyyaml>=6.0
pydantic>=2.6
python-dotenv>=1.0
tqdm>=4.66
rich>=13.7

# Hash core (M2/M3)
torch>=2.2,<3
transformers>=4.45
sentencepiece>=0.2
accelerate>=0.30
Pillow>=10.0
faiss-cpu>=1.8

# Ingestion (M1) — RAG-Anything pulls in LightRAG, MinerU, huggingface_hub, tqdm
raganything>=1.3
lightrag-hku

# Agent (M4)
langgraph>=0.2.50
langchain-core>=0.3

# MCP (M6)
mcp>=1.2

# Eval (M7)
pandas>=2.2
matplotlib>=3.8
scikit-learn>=1.4
pytrec_eval>=0.5
datasets>=2.20
