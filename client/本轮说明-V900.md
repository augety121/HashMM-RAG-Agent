# HashMM V900 本轮说明

## 已实现

本轮按 `docs/CONTROL_PLANE_SETTINGS_PLUGINS_V900_SPEC.md` 完成管理后台、设置和插件中心的主链收口：个人资料与账号偏好改用 owner-scoped 自助 API；设置写入具有 revision、事务、审计和服务端回执；密码旋转接收新令牌；管理总览逐数据源报告可验证性；运行时 secret 加密落库；插件 ZIP 经过双侧摘要、严格解包、显式审核、精确信任、诊断、撤销和升级隔离。

普通用户不再被送入管理员壳层。ProjectVault 规则保持不变：稳定数据位于用户选择的安装目录下 `HashMM Data`，没有改成固定 C 盘路径。本轮没有安装 PaddleOCR、Unlimited OCR 或任何新 pip 包，也没有修改 `requirements.txt`。

## 已执行验证

- Python 全量回归按 215 个测试文件四片执行：`498 + 423 + 349 + 369 = 1639 passed`，`6 skipped`，`0 failed`。
- 新增设置/插件定向回归与既有插件安全回归：`13 passed`。
- Frontend 全量 Vitest：`57 files / 214 passed`。
- Frontend TypeScript：`npm run typecheck` 通过。
- Next.js production build：编译、类型检查、静态导出通过。
- Desktop MCP 真实子进程握手、工具发现、调用与权限门：`6/6` 通过。
- Release source gate：Desktop `2.3.0`、Backend `V900`、现有 runtime 与 WebUI 一致。
- 版本更新后的发布/设置/插件安全定向回归：`33 passed`。
- 原生唯一发布流水线完成；EXE 实际 SHA-256 与 `.sha256`、`.release.json` 三方一致。
- 服务器 ZIP 可直接导入 V900，并在临时旧审批库上验证加法升级与历史记录保留。

## 交付产物

- 服务器包：`release-server/hashmm-server-V900-20260809-085616.zip`
  - 字节数：`6,334,983`
  - SHA-256：`5fbdf6f6a9cbb541ac4598b1495aa90b20dba0ef35edae1b71cfdbf5aca62ed9`
- Windows 安装器：`installer-native/HashMM-Setup.exe`
  - 字节数：`285,786,144`
  - SHA-256：`a15baa631eae84f09dec15fa87a9b6cebb59160d82705ffe21a22e227bc61b0b`
  - Authenticode：`NotSigned`；适合内部验收，不应冒充已签名公开发行版。

## 没有伪装为已完成的事项

- Python 插件审核后仍与后端同进程，不是容器沙箱。
- 没有发布者签名、插件市场签名链或恶意代码检测。
- 管理角色仍是 admin/user 两级，不是完整组织级 RBAC。
- Supabase 资料写入需要部署方的 profiles 表及 RLS 正确配置。
- 本地 Windows 构建证明不了真实公网、外部 Supabase、第三方 API 或生产负载已经验收。
- SHA-256 只证明产物字节一致，不等同于 Authenticode 发布者身份。

## 版本

- Backend/SDK：`V900 / 0.9.0`
- Desktop/WebUI/MCP/Native：`2.3.0`
