# Copy to .env, chmod 600, and never commit it.
# Pin a reviewed tag/digest; do not use latest in production.
HASHMM_COTURN_IMAGE=coturn/coturn:REPLACE_WITH_PINNED_VERSION
HASHMM_TURN_CERT_DIR=/etc/letsencrypt/live/turn.example.com

