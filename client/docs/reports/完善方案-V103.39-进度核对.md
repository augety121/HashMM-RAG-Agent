# 完善方案 V103.39 — 实现进度核对（截至 V103.52）

> 逐条对照原方案的 P0–P3，标注**已做 / 部分 / 未做**，并给出代码依据（文件:行 或 grep 结论）。
> 结论先行：**P0 基本完成，P1 已落主路径，P2 大部分到位，P3 用「预设」方式解决了「默认打开」**。

---

## P0（地基）—— ✅ 基本完成

| 子项 | 状态 | 依据 |
|---|---|---|
| 评估门接进 CI | ✅ | `ci/backend-ci.yml` 跑 `python -m hashmm.evaluation.gate --stub`（4 处引用），并已加 remote_hub / remote-extensions / 高亮回归 |
| 质量门升级（关键词→质量） | ✅ | V103.51 新增 `hashmm/evaluation/llm_judge.py`（四维 rubric）+ 接进 `gate.py`，`--judge` 默认开、降级安全 |
| 前端代码分割 / 懒加载 | ✅ | `DesktopPanel.tsx` 13 处 `dynamic()`；`AdminPanel.tsx` 11 处 —— 方案说的「0 处」已补齐 |
| 错误边界 ErrorBoundary | ✅ | `ErrorBoundary.tsx` 存在，被 `App.tsx` / `AdminPanel.tsx` / `DesktopPanel.tsx`（每个面板按 `key={view}` 包裹）使用 |
| 加载/空/错 三态 | ✅ | `StateBlock.tsx`（V103.42）→ V103.52 升级为 `ui/PanelKit.tsx` 的 `StateView`，13 面板统一接入 |
| 无障碍 aria | ✅（基线） | PanelKit 组件统一带 `role`/`aria-*`（Toggle role=switch、StateView role=status/alert、按钮 aria-label） |

## P1（最大智能收益：答案质量到主路径）—— ✅ 已落主路径

主路径 = `hashmm/api/streaming.py` 的 `generate_sse_async`。核对实际接线：

| 子项 | 状态 | 依据（streaming.py） |
|---|---|---|
| `context_pack` 相关性装箱 | ✅ | 第 24 行 `from hashmm.agent.context_pack import clip_at_boundary`（边界裁剪在主路径用） |
| `CRAG` 纠错检索 | ✅ | 第 855–868 行「Step 1 (CRAG corrective): rewrite + re-search LOCAL KB」，弱检索时触发 `_corrective_retrieval`（第 1407 行定义） |
| `confidence` 置信门控 | ✅（代码已接，默认随 preset） | 第 811–829 行「confidence-gated escalation」：低置信→多跳补检索；开关 `HASHMM_CONFIDENCE`（basic 关 / max 开） |
| `conv_compact` 长对话压缩 | ✅ | 第 192–195、1221 行：取足历史交 `compact_history` 确定性压缩 |
| `orchestrator` 子智能体 | ✅ | 第 532–631 行：`SubAgentOrchestrator` 在主路径编排、`subagent` 事件流式输出 |
| `evaluator_optimizer` 答案精炼 | ⚠️ 部分 | 评判器以 `HASHMM_VERIFIER` 开关存在（preset=max 开）；主路径默认走置信门控+CRAG，精炼按需 |

## P2（上下文工程 + 补 KG 社区）—— ✅ 大部分到位

| 子项 | 状态 | 依据 |
|---|---|---|
| `context_manager` 高级长程 | ⚠️ 部分 | `HASHMM_CONTEXT_COMPACTION` 开关（preset=max）；主路径默认用 conv_compact，足够长对话不失忆 |
| 统一上下文预算装箱 | ✅ | context_pack 的 clip/装箱在主路径 |
| KG 社区检测（补 0 社区） | ✅ 能力就绪 | `hashmm/kg/community.py` + `community_retrieval.py` 存在；质量看板/主动发现都有「一键构建社区检索」按钮（`rebuildCommunities` → `/api/kg/rebuild-communities`），把「0 社区」一键补上 |
| `KG_RETRIEVAL` 策略 | ✅（开关） | `hashmm/kg/kg_retrieval.py`，`HASHMM_KG_RETRIEVAL` 控制 |

## P3（越用越聪明默认开 + 客户端工程化 + 架构债）—— ✅ 以「预设」解决「默认打开」

**「默认打开」的落地方式（比逐个翻默认更安全）**：`hashmm/feature_presets.py` 提供三档预设，
`config.py:29-31` 启动时 `apply_preset()` 一次：
- `HASHMM_PRESET=basic`（默认）= 历史行为，零风险；
- `recommended` = 低风险增益（缓存/时间线/审计/**用户记忆**/记忆服务）一键开；
- `max` = 满血（+ 评判器/HyDE/多查询/agentic 检索/上下文压缩/LLM 路由…）。
- 原则：**显式优先**（用户已设的 `HASHMM_X` 绝不被预设覆盖）、**只设开关不碰本体**、异常不影响启动。

| 子项 | 状态 | 依据 |
|---|---|---|
| USER_MEMORY / exp_rules / skill 默认可开 | ✅ | `feature_presets`：USER_MEMORY 进 recommended 档；exp_rules/verifier 进 max 档；记忆中心面板可读/删/教 |
| KG 社区→global 检索 | ✅ 能力就绪 | 见 P2 |
| loop 面板「默认真起作用」 | ✅ | 记忆中心/自我进化/主动发现/定时任务面板均已上客户端，且主动发现可「发现→一键交付」 |
| 客户端三态/无障碍 | ✅ | PanelKit StateView + aria（见 P0） |
| 客户端设计系统统一 | ✅ V103.52 新增 | `ui/PanelKit.tsx`：PageHeader/Card/StatCard/Button/Badge/Field/Toggle/StateView/CardGrid，9 个系统面板已统一重构 |
| 遥测打通 | ✅ | 运行轨迹面板（`HASHMM_AGENT_TRACE`）+ `run_record.py` |
| 架构债：两条路径合并 | ⚠️ 部分 | 主路径已吸收 CRAG/context_pack/orchestrator/confidence；`/api/chat/agentic` 旁路仍在（保留兼容），完全废弃待后续 |

---

## 仍可继续的（诚实列出）

1. **evaluator_optimizer / context_manager 默认接主路径**：目前靠 `HASHMM_PRESET=max` 开；可评估后并入 recommended。
2. **彻底合并两条路径**：把 `/api/chat/agentic` 旁路废弃或并入主 `generate_sse_async`，消除长期分叉。
3. **KG 社区**：能力就绪，但需在真机用真语料跑一次 `rebuild-communities` 把生产库的「0 社区」补上（沙箱无模型跑不了）。

> 一句话：方案的「把已造好但休眠的能力，评估护栏下接进主路径并默认可开」——**主体已完成**，
> 剩下的是「把 max 档里两个重模块下放到 recommended」和「废弃旁路端点」这类收尾，需要真机 eval 数据支撑。
