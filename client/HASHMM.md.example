# HASHMM.md · 项目指令（示例模板）

> 这是 HashMM 的项目级指令文件（对标 Claude Code 的 CLAUDE.md / Codex 的 AGENTS.md）。
> agent 在每次任务前会自动加载本文件并注入系统提示，**让你不改代码就能定制 agent 的全局行为**。
>
> 放置位置（就近优先）：当前工作目录 > 项目根。也可用环境变量 `HASHMM_PROJECT_INSTRUCTIONS` 指定路径。
> 删掉本文件或留空 = agent 行为和默认完全一样（零影响）。

## 回答风格
- 用中文回答，技术术语可保留英文。
- 先给结论，再展开论证。
- 不使用 emoji。

## 领域约束（按需定制）
- 所有财务数据必须标注来源文档和页码。
- 涉及具体产品/公司时，先检索知识库核对，不凭记忆断言。

## 工具偏好
- 用户要 PPT/Word 时直接用 create_document 生成文件，不要给制作步骤。
- 检索够用就停（每主题 1-2 次），不要反复搜索。

## 禁止事项
- 不编造数据。缺数据时说明缺什么。
- 不输出与任务无关的客套话。

<!--
把上面的内容改成你项目的真实规则即可。例如：
- 公司内部知识库 → 加入术语表、部门缩写、合规要求
- 特定领域（医疗/法律/金融）→ 加入该领域的免责声明和引用规范
-->
