# HashMM core dependencies (required to run the RAG-Agent backend).
# Generated from real import scan. Lower bounds only, no upper pins.
# Install: pip install -r requirements.txt
# Optional/heavy deps live in requirements-optional.txt (GPU models, doc export...).
# NOTE: keep this file ASCII-only. pip decodes requirements with the system
#       locale when there is no BOM (GBK/cp936 on zh-CN Windows), so any
#       non-ASCII byte breaks local installs. Chinese notes: see CONFIG.md.

# -- Web framework / API --
fastapi>=0.110
starlette>=1.3.1
uvicorn[standard]>=0.27
pydantic>=2.0
python-multipart>=0.0.9
PyYAML>=6.0
# Windows embedded Python does not ship the IANA timezone database. Scheduled
# work must keep the user's wall-clock time across desktop and server runtimes.
tzdata>=2026.1

# -- Retrieval core --
numpy>=1.24
faiss-cpu>=1.7
rank_bm25>=0.2.2
jieba>=0.42
networkx>=3.0

# -- HTTP / LLM clients --
httpx>=0.27
requests>=2.31
openai>=1.30

# -- Document parsing (corpus ingest) --
pypdf>=6.13.3
pdfplumber>=0.10
Pillow>=12.3.0
python-pptx>=0.6.23
python-docx>=1.1
openpyxl>=3.1

# -- Auth --
PyJWT>=2.8
cryptography>=42
